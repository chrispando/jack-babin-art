import React from "react";
import Gallery from "../components/Gallery";

const GalleryScreen = () => {
  return (
    <div>
      <Gallery />
    </div>
  );
};

export default GalleryScreen;
